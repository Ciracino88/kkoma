import type { CSSProperties } from 'react'

/** JetBrains Mono 폰트 인라인 스타일 (숫자/라벨용) */
export const MONO: CSSProperties = { fontFamily: "'JetBrains Mono', monospace" }

export type Filter = 'all' | 'mp3' | 'mp4'
